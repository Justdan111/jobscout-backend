# Server
PORT=8080
DATA_DIR=./data
ALLOWED_ORIGIN=http://localhost:3000

# Relevance scoring + application drafting (optional but recommended).
# Without this, discovery still works via keyword scoring; drafting is disabled.
ANTHROPIC_API_KEY=
JOBSCOUT_MODEL=claude-haiku-4-5-20251001

# Email digest (optional) — free key at resend.com
RESEND_API_KEY=
DIGEST_TO=you@example.com
DIGEST_FROM=JobScout <onboarding@resend.dev>
